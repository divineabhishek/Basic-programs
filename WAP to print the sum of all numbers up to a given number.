#include <stdio.h
#include <conio.h>
#include <math.h>

int main(){
int n , sum ;

printf("Enter the number up to which you want the sum:\n");
scanf("%d", &n);

sum = n * (n + 1)/2;
printf("The sum of %d numbers is %d.\n", n , sum);

getch();
}
